# Galactic Bridge

Single-page sci-fi command deck inspired by a cinematic starship console. It is purely visual: animated radar, star map, shield dial, pulsing meters, shifting coordinates, and cosmetic control buttons.

## Run

Open `index.html` directly in a browser, or serve the folder with:

```bash
python3 -m http.server 8787
```
